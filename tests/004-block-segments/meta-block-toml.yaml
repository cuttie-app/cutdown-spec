id: meta-block-toml
section: "4.3"
description: TOML format meta block stores raw content with format=toml

input: |
  ~~~toml
  title = "Hello"
  ~~~

pages:
  - meta:
      type: Meta
      format: toml
      raw: "title = \"Hello\"\n"
