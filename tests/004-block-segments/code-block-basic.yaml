id: code-block-basic
section: "4.4"
description: Fenced code block with no language tag defaults to language "text"

input: |
  ```
  hello world
  ```

ast:
  - type: CodeBlock
    language: text
    raw: "hello world"
