id: heading-inline-variable
section: "4.2"
description: Heading text is parsed by inline rules; a variable inside a heading is resolved as a Variable node

input: |
  = Title {{var}}

ast:
  - type: Section
    level: 1
    heading:
      - type: Text
        value: "Title "
      - type: Variable
        key: var
