id: code-block-literal
section: "4.4"
description: Content inside a CodeBlock is literal; inline markup is not parsed

input: |
  ```
  **bold** and __strong__
  ```

ast:
  - type: CodeBlock
    language: text
    content: "**bold** and __strong__"
