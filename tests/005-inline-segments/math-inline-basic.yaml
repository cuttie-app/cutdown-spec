id: math-inline-basic
section: "5.7"
description: "$$ delimiters produce MathInline with literal formula; single $ is literal text"

input: |
  $$a^2 + b^2$$

ast:
  - type: Paragraph
    children:
      - type: MathInline
        formula: a^2 + b^2
