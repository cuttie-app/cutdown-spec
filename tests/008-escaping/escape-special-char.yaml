id: escape-special-char
section: "8"
description: Backslash before special character emits the literal character; backslash is consumed

input: |
  \*

ast:
  - type: Paragraph
    children:
      - type: Text
        value: "*"
