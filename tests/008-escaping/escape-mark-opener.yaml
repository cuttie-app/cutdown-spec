id: escape-mark-opener
section: "8.2"
description: "\\: before a Mark opener suppresses it; the colon is literal and no Mark is created"

input: |
  \::a b::

ast:
  - type: Paragraph
    children:
      - type: Text
        value: "::a b::"
