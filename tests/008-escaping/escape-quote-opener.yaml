id: escape-quote-opener
section: "8.2"
description: "`\\>` at line start suppresses QuoteBlock; line becomes Paragraph"

input: |
  \> not a quote

ast:
  - type: Paragraph
    children:
      - type: Text
        value: "> not a quote"
