id: escape-codeblock-closer
section: "8.3"
description: "`\\`` inside CodeBlock body emits literal backtick; escaped triple-backtick line does NOT close the fence"

input: |
  ```text
  before
  \```
  after
  ```

ast:
  - type: CodeBlock
    language: "text"
    raw: "before\n```\nafter"
